<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Berry Blog</title>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="body-div">
    <header class="header-div">
      <h1>Welcome to the berry blog!</h1>
      <button><a class="href" href="/">Homepage</a></button>
      <nav class="nav-div">
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <button><a class="href" href="/profile">Profile</a></button> |
        <button class="logout" id="logout">Logout</button>
        {{else}}
        <button><a class="href" href="/login">Login</a></button>
        {{/if}}
      </nav>
    </header>
    <main class="body-maindiv">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>

    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>